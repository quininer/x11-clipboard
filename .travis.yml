language: rust
rust:
    - stable
cache: cargo
os:
    - linux
sudo: required
services:
    - xvfb
before_script:
    - sudo apt-get update -qq
    - sudo apt-get install -y libx11-xcb-dev
script:
    - cargo test
